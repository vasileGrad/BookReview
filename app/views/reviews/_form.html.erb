<%= simple_form_for([@book, @book.reviews.build]) do |f| %>
	<h3>
		<div id="rating-form">
			<label>Rating</label>
		</div>
	</h3>
	<%= f.input :comment %>
	<%= f.button :submit %>
<% end %>

<script>
	$('#rating-form').raty({
		path: '/assets',
		scoreName: 'review[rating]'
	});
</script>